# Copy this file to `.env.local` (recommended) or `.env` and fill in values.
# Vite only exposes variables prefixed with VITE_ to the client.
# WARNING: VITE_* values are bundled into the frontend and are publicly visible.

# GitHub username used by Stats section
VITE_GITHUB_USERNAME=

# Optional token for GitHub GraphQL (commit contributions).
# Use a low-privilege token. Avoid any write/admin scopes.
VITE_GITHUB_TOKEN=

# Contact form (Web3Forms)
# Create an access key at https://web3forms.com/ and paste it here.
VITE_WEB3FORMS_ACCESS_KEY=
